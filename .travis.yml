language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-nglib' ]; then cd .. && eval "mv $currentfolder generator-nglib" && cd generator-nglib; fi
